# sec-8k-corpus — session notes

- This public repo holds only public SEC data and the scripts that fetch, build and verify it.
- Operational details (servers, hosts, access paths) are NOT documented here. They live in the owner's private repository. If the owner mentions "Hetzner", read `HETZNER.md` in the private `taxonomy-factory` repo.
